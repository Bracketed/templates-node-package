{
	"name": "@bracketed/package",
	"packageManager": "yarn@4.4.1",
	"version": "1.0.0",
	"publishConfig": {
		"access": "public"
	},
	"description": "Description",
	"author": "@bracketed",
	"main": "./lib/cjs/index.js",
	"module": "./lib/esm/index.js",
	"types": "./types/index.d.ts",
	"exports": {
		".": {
			"require": "./lib/cjs/index.js",
			"import": "./lib/esm/index.js"
		}
	},
	"files": [
		"lib",
		"types"
	],
	"type": "module",
	"imports": {
		"#root/*": "./lib/*.js"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/bracketed/templates-package.git",
		"directory": "."
	},
	"homepage": "https://bracketed.co.uk",
	"sideEffects": false,
	"bugs": {
		"url": "https://github.com/bracketed/templates-package/issues"
	},
	"keywords": [
		"bracketed",
		"typescript"
	],
	"scripts": {
		"build:cjs": "tsc -p tsconfig.cjs.json",
		"build:esm": "tsc -p tsconfig.esm.json",
		"build:types": "tsc -p tsconfig.types.json",
		"build": "yarn build:cjs && yarn build:esm && yarn build:types",
		"build:production": "npm run build:cjs && npm run build:esm && npm run build:types",
		"build:lint": "yarn build && yarn lint",
		"lint": "yarn prettier --write lib/**/*.js && yarn prettier --write types/**/*.d.ts && yarn prettier --write types/**/*.d.ts.map",
		"validate": "yarn prettier --check src/**/*.ts",
		"lint:all": "yarn prettier --write .",
		"validate:all": "yarn prettier --check ."
	},
	"devDependencies": {
		"@bracketed/typescript-config": "^4.4.0",
		"@types/node": "^20.10.5",
		"prettier": "^3.1.1",
		"typescript": "^5.3.3"
	},
	"dependencies": {
		"tslib": "^2.7.0"
	}
}
